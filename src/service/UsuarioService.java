package service;

import model.Usuario;

public class UsuarioService {
    public boolean autenticar(String email, String senha) {
        // Verifica credenciais
        return true;
    }

    public void registrarUsuario(Usuario usuario) {
        // Valida e salva novo usuário
    }
}
